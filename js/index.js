window.onload = () => {
  document.getElementById('start-button' || 'end-button').onclick = () => {
    startGame();
  };

  function startGame() { }

  function endGame() { }
};


window.onload = () => {
  document.getElementById('start-button' || 'end-button').onclick = () => {
    startGame();
  };

  function startGame() { }

  function endGame() { }
};
window.onload = () => {
  document.getElementById('start-button' || 'end-button').onclick = () => {
    startGame();
  };

  function startGame() { }

  function endGame() { }
};
window.onload = () => {
  document.getElementById('start-button' || 'end-button').onclick = () => {
    startGame();
  };

  function startGame() { }

  function endGame() { }
};
